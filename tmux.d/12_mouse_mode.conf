# vim:set ft=tmux:
<% helper = TmuxERBParser::Helpers %>
<% if helper.tmux_version < 2.1 %>
set -gw mode-mouse on
set -g mouse-utf8 on
set -g mouse-select-pane on
set -g mouse-select-window on
set -g mouse-resize-pane on
<% else %>
set -g mouse on
<% end %>
